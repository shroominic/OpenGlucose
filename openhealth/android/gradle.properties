org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Flutter 3.41.6 and its locked Android plugins still apply KGP and legacy
# AGP DSL/variant APIs. Preserve the documented AGP 9 migration bridge until
# the pinned Flutter toolchain and plugins no longer require it.
android.builtInKotlin=false
android.newDsl=false
